export default class ElementTableau {
    constructor (Nom, Prix, Stock, Commandes = false) {
        this._texte = texte;
        this._fait = fait;
        this._id = Date.now() + Math.floor(Math.random()*10) // génération d'un id
        }
    }